# Drag-Drop-React-Form-Builder
A drag-and-drop form builder built with React, JavaScript, and raw CSS. Users can add and customize fields like text inputs, dropdowns, tables, and file uploads. The app supports reordering, deletion, and conditional logic, with a separate preview page to visualize the final form layout.
